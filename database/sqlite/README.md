# SQLite local database

This project includes a base SQLite schema for local prototyping and testing.

## Initialize

From the project root:

```bash
node scripts/init-sqlite.mjs
```

This creates the database file:

```text
data/careflow.db
```

The schema is defined in:

```text
database/sqlite/schema.sql
```
